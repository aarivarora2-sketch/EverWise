# Keep the real value in .env.local. Never commit it.
ELEVENLABS_API_KEY=replace_with_a_new_elevenlabs_api_key
OPENAI_API_KEY=replace_with_a_new_openai_api_key

# Optional: set this only when production uses a separately hosted API.
# VITE_READ_ALOUD_ENDPOINT=https://your-api.example.com/api/read-aloud
# VITE_CHECK_MESSAGE_ENDPOINT=https://your-api.example.com/api/check-message
